# Research data: raw dumps stay local, findings live in NOTES.md
data/
snapshots/
*.parquet
*.jsonl

# Build
target/

# Spy identity: a persistent research identity (ETHICS.md), not code.
# One key per machine, generated on first run, never committed.
spy/keypair.json
keypair.json

# Secrets / OS
.env
.DS_Store

# Spy run logs: local diagnostics, not findings
logs/
